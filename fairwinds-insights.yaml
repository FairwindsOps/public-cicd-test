images:
  folder: ./tmp/images
  docker:
    - busybox
manifests:
  folder: ./tmp/manifests
  yaml:
    - ./failing.yaml
    - ./passing.yaml
  helm:
    - name: helm-release-pruner
      path: ./chart
      variableFile: ./chart/values.yaml

options:
  tempFolder: ./tmp/options
  junitOutput: ./tmp/junit
  organization: acme-co
  fail: true
  repositoryName: fairwindsops/cicd-test
